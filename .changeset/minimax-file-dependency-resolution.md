---
"@fancyrobot/fred-minimax": patch
---

Remove the workspace-only Fred dev dependency from the MiniMax provider manifest so the package can be consumed as a local `file:` dependency outside the Fred monorepo.
