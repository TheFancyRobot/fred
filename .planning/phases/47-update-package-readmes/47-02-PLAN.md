---
phase: 47-update-package-readmes
plan: 02
type: execute
wave: 2
depends_on: ["47-01"]
files_modified:
  - README.md
  - packages/dev/README.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Root README is a slim hub (~80-100 lines) linking to package READMEs for details"
    - "Root README quickstart uses .md agent file approach as primary"
    - "Root README does not duplicate content that now lives in package READMEs"
    - "Dev README includes server mode endpoint table from root"
    - "All cross-links between READMEs resolve correctly"
  artifacts:
    - path: "README.md"
      provides: "Slim hub README linking to packages"
    - path: "packages/dev/README.md"
      provides: "Updated dev README with server mode section"
  key_links:
    - from: "README.md"
      to: "packages/core/README.md"
      via: "relative link in packages table and quickstart"
      pattern: "packages/core/README"
    - from: "README.md"
      to: "packages/cli/README.md"
      via: "relative link in packages table"
      pattern: "packages/cli/README"
    - from: "README.md"
      to: "provider READMEs"
      via: "packages table links"
      pattern: "packages/provider-"
    - from: "README.md"
      to: "examples/README.md"
      via: "examples link"
      pattern: "examples/README"
---

<objective>
Slim the root README into a hub-style overview (~80-100 lines) and update the dev README with server mode content. The root README links to package READMEs for all detail.

Purpose: Root README becomes the entry point directing readers to the right package README. Server mode documentation moves to the dev package where the server code lives.

Output: Updated README.md (slimmed) and packages/dev/README.md (server mode added)
</objective>

<execution_context>
@/home/gimbo/.claude/get-shit-done/workflows/execute-plan.md
@/home/gimbo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/47-update-package-readmes/47-CONTEXT.md
@.planning/phases/47-update-package-readmes/47-RESEARCH.md
@.planning/phases/47-update-package-readmes/47-01-SUMMARY.md
@README.md
@packages/dev/README.md
@packages/core/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Slim root README to hub-style overview</name>
  <files>README.md</files>
  <action>
Rewrite the root README.md as a slim hub. Target ~80-100 lines. Read the current README.md first.

Structure per user decisions:

```
# Fred

[![npm version](https://img.shields.io/npm/v/@fancyrobot/fred)](https://www.npmjs.com/package/@fancyrobot/fred)
[![CI](https://github.com/TheFancyRobot/fred/actions/workflows/ci.yml/badge.svg)](https://github.com/TheFancyRobot/fred/actions/workflows/ci.yml)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

{Brief intro paragraph: what Fred is, built on Effect + Bun}

## Features

{Slim bullet list of features -- what Fred does at a high level, not how}

## Quick Start

Show .md agent file approach as PRIMARY (create agent file, config.yaml, TypeScript entrypoint). Short and focused. Add "See the [core package documentation](packages/core/README.md) for the full getting-started guide and programmatic API."

## Packages

Table linking to each package README:

| Package | Description |
|---------|-------------|
| [@fancyrobot/fred](packages/core/README.md) | Core framework |
| [@fancyrobot/fred-cli](packages/cli/README.md) | CLI and TUI |
| [@fancyrobot/fred-dev](packages/dev/README.md) | Development tooling |
| [@fancyrobot/fred-openai](packages/provider-openai/README.md) | OpenAI provider |
| [@fancyrobot/fred-anthropic](packages/provider-anthropic/README.md) | Anthropic provider |
| [@fancyrobot/fred-google](packages/provider-google/README.md) | Google (Gemini) provider |
| [@fancyrobot/fred-groq](packages/provider-groq/README.md) | Groq provider |
| [@fancyrobot/fred-openrouter](packages/provider-openrouter/README.md) | OpenRouter provider |

## Examples

Brief mention of 12 progressive examples + link to examples/README.md.

## Documentation

Link to docs site: sincspecv.github.io/fred

## Contributing

Keep brief: clone, install, test, build commands. Link to CONTRIBUTING.md.

## License

MIT
```

Content that MUST be removed from root (now in package READMEs):
- Detailed provider table with backend column (moved to core README)
- Full CLI command list (already in CLI README)
- Server mode section (moved to dev README)
- Development chat section (already in dev README)
- Config file example (moved to core README)
- Detailed quick start with tool registration and createAgent (moved to core README)

Content that STAYS in root:
- Brief intro and feature bullets (high-level what Fred does)
- Quick start showing .md agent file approach (slim version)
- Packages table with links
- Documentation link
- Contributing (brief)
- License

Per user decision: no provider installation details like `@effect/ai-openai` in root. Root just lists package names with links.
  </action>
  <verify>
    <automated>wc -l README.md && echo "---" && grep -c "packages/" README.md && echo "package links found" && ! grep -q "@effect/ai-openai\|@effect/ai-anthropic\|@effect/ai-google" README.md && echo "No backend deps leaked" && grep -q "packages/core/README.md" README.md && echo "Core link present"</automated>
  </verify>
  <done>Root README is ~80-100 lines with brief intro, feature bullets, .md agent quickstart, packages table with links to all 8 package READMEs, examples link, docs link, contributing, and license. No duplicated content that now lives in package READMEs. No backend dependency details.</done>
</task>

<task type="auto">
  <name>Task 2: Add server mode section to dev README and verify all cross-links</name>
  <files>packages/dev/README.md</files>
  <action>
Read the current packages/dev/README.md. Add a "Server Mode" section with the endpoint table that was removed from the root README. Place it after the existing "Usage" section.

Add this section:

```
## Server Mode

Start the development server:

\`\`\`bash
bun run server
# Or with options:
bun run server --config config.yaml --port 3000
\`\`\`

The server exposes an OpenAI-compatible API:

| Endpoint | Description |
|----------|-------------|
| `POST /v1/chat/completions` | OpenAI-compatible chat (works with Chatbox, etc.) |
| `POST /chat` | Simplified chat endpoint |
| `POST /message` | Process with intent matching options |
| `GET /agents` | List agents |
| `GET /intents` | List intents |
| `GET /tools` | List tools |
| `GET /health` | Health check |
```

Also add a "Related" section at the bottom (before License) linking back to the core README and root README, matching the pattern used in provider READMEs.

After writing, verify all cross-links across the full README set by checking that relative link targets exist:
- Root README links to packages/core/README.md, packages/cli/README.md, packages/dev/README.md, packages/provider-*/README.md, examples/README.md
- Core README links to ../provider-*/README.md, ../../examples/README.md
- Provider READMEs link to ../core/README.md, ../../README.md
- Dev README links to ../core/README.md, ../../README.md
  </action>
  <verify>
    <automated>grep -q "Server Mode" packages/dev/README.md && echo "Server section present" && grep -q "/v1/chat/completions" packages/dev/README.md && echo "Endpoints table present" && echo "--- Cross-link check ---" && for f in packages/core/README.md packages/cli/README.md packages/dev/README.md packages/provider-openai/README.md packages/provider-anthropic/README.md packages/provider-google/README.md packages/provider-groq/README.md packages/provider-openrouter/README.md examples/README.md; do test -f "$f" && echo "$f: EXISTS" || echo "$f: MISSING"; done</automated>
  </verify>
  <done>Dev README has server mode section with endpoint table. All cross-links between READMEs point to existing files. The complete README set is internally consistent.</done>
</task>

</tasks>

<verification>
- Root README is ~80-100 lines (hub style, not detailed)
- Root README quickstart shows .md agent file approach
- Root README has packages table linking to all 8 package READMEs
- Dev README has server mode endpoint table
- All relative cross-links between READMEs resolve to existing files
- No content is duplicated between root and package READMEs
</verification>

<success_criteria>
- Root README slimmed from 221 lines to ~80-100 lines
- Root README links to all package READMEs and examples
- Dev README includes server mode section
- All cross-links verified as pointing to existing files
- No provider backend implementation details in root README
</success_criteria>

<output>
After completion, create `.planning/phases/47-update-package-readmes/47-02-SUMMARY.md`
</output>
