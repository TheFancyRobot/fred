---
phase: 46.2-gap-closure-agents-convention
plan: 01
subsystem: examples
tags: [examples, eta, template-context, config-initializer, docs]

requires:
  - phase: 46.1-refactor-examples-md-agents-eta-templates
    provides: markdown-agent examples and ETA baseline behavior
provides:
  - Example 04 department loop now reads runtime template context (`it.departments.available`)
  - Config initializer default discovery now prefers `./src/agents` before `./agents`
  - Examples documentation now reflects `src/agents/*.md` convention and context-driven ETA guidance
affects: [46.2-02, 46.2-03, 46.2-04, examples-guard]

tech-stack:
  added: []
  patterns:
    - Runtime ETA loop data is injected via `fred.addTemplateContext(...)` instead of hardcoded prompt literals
    - Default agent directory convention is `src/agents` with legacy `agents` fallback

key-files:
  created: []
  modified:
    - examples/04-dynamic-handoff/src/index.ts
    - examples/04-dynamic-handoff/agents/intake.md
    - packages/core/src/config/initializer.ts
    - examples/README.md

key-decisions:
  - "Example 04 retains specialist IDs and handoff behavior while moving list ownership to runtime template context."
  - "ConfigInitializer default discovery now checks ./src/agents first, then ./agents, while preserving explicit agentDirs precedence."
  - "Examples docs now treat src/agents as the documented convention and explain Example 04 ETA data-context usage."

patterns-established:
  - "ETA loops in prompts should consume injected context data (`it.*`) rather than embedding source-of-truth literals."
  - "Example conventions align agent markdown location with TypeScript project structure under src/."

duration: 2 min
completed: 2026-03-04
---

# Phase 46.2 Plan 01: Agents convention and ETA source-of-truth Summary

**Example 04 now uses runtime template context for specialist-loop data, while initializer defaults and docs align on the `src/agents` convention.**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-04T03:07:11Z
- **Completed:** 2026-03-04T03:09:57Z
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments
- Replaced the Example 04 hardcoded ETA specialist loop source with runtime `departments.available` context injected through `fred.addTemplateContext('departments', ...)`.
- Updated `ConfigInitializer` fallback directory detection to prefer `./src/agents`, then legacy `./agents`, with no behavior change when `config.agentDirs` is explicitly set.
- Updated `examples/README.md` to document `src/agents/*.md`, remove root-level `agents/` justification, and call out Example 04's context-driven ETA usage.

## Task Commits

Each task was committed atomically:

1. **Task 1: Replace Example 04 inline ETA array with runtime template context** - `5c7ee33` (feat)
2. **Task 2: Update ConfigInitializer default agent directory priority** - `bfe5181` (fix)
3. **Task 3: Align examples documentation with src/agents convention** - `3e8fd5c` (docs)

_Plan metadata commit: pending at summary creation time._

## Files Created/Modified
- `examples/04-dynamic-handoff/src/index.ts` - Adds runtime `departments` template context before config initialization.
- `examples/04-dynamic-handoff/agents/intake.md` - ETA loop now iterates `it.departments.available`.
- `packages/core/src/config/initializer.ts` - Default discovered agent directory priority now prefers `src/agents`.
- `examples/README.md` - Documents `src/agents` convention and Example 04 context-driven ETA loop.

## Decisions Made
- Kept specialist IDs and handoff flow unchanged, changing only the source of truth for the loop data.
- Preserved backward compatibility by keeping `./agents` as fallback after `./src/agents`.
- Removed legacy doc rationale for root-level `agents/` to avoid teaching an outdated convention.

## Deviations from Plan

None - plan executed exactly as written.

## Authentication Gates

None.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Ready for `46.2-02-PLAN.md` (migrate examples 01-04 from `agents/` to `src/agents/`).
- No blockers from this plan.

---
*Phase: 46.2-gap-closure-agents-convention*
*Completed: 2026-03-04*
