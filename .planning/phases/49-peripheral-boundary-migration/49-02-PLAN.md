---
phase: 49-peripheral-boundary-migration
plan: 02
type: execute
wave: 2
depends_on: [49-01]
files_modified:
  - packages/core/src/observability/otel.ts
  - packages/core/src/observability/context.ts
  - packages/core/src/effect/index.ts
autonomous: true
requirements: [CONS-04]

must_haves:
  truths:
    - "JSDoc examples in otel.ts, context.ts, and effect/index.ts show idiomatic Effect patterns instead of runPromise"
    - "Full test suite passes with no regressions after doc changes"
    - "Build succeeds cleanly"
  artifacts:
    - path: "packages/core/src/observability/otel.ts"
      provides: "Idiomatic Effect JSDoc examples"
    - path: "packages/core/src/observability/context.ts"
      provides: "Idiomatic Effect JSDoc examples"
    - path: "packages/core/src/effect/index.ts"
      provides: "Idiomatic Effect JSDoc examples"
  key_links: []
---

<objective>
Rewrite JSDoc examples in observability and effect modules to show idiomatic Effect patterns instead of Effect.runPromise, and run final full-suite verification.

Purpose: Ensure documentation reflects the project's boundary-only runPromise policy. Final verification confirms CONS-04 is fully satisfied.
Output: Updated JSDoc examples, clean full test suite run.
</objective>

<execution_context>
@/home/gimbo/.claude/get-shit-done/workflows/execute-plan.md
@/home/gimbo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-peripheral-boundary-migration/49-CONTEXT.md
@.planning/phases/49-peripheral-boundary-migration/49-01-SUMMARY.md
@packages/core/src/observability/otel.ts
@packages/core/src/observability/context.ts
@packages/core/src/effect/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite JSDoc examples to idiomatic Effect patterns</name>
  <files>packages/core/src/observability/otel.ts, packages/core/src/observability/context.ts, packages/core/src/effect/index.ts</files>
  <action>
Find all JSDoc `@example` blocks in these three files that contain `Effect.runPromise` or `Runtime.runPromise` patterns. Rewrite each example to show idiomatic Effect composition instead:

- Replace `await Effect.runPromise(program)` with Effect composition patterns like `Effect.gen`, `pipe`, `Effect.flatMap`
- Show the example as a composable Effect pipeline rather than an imperative boundary call
- If an example needs to show running an Effect (for completeness), use a comment like `// Run at application boundary: Effect.runPromise(program)` rather than showing it as the primary pattern
- Keep the same semantic content and teaching purpose of each example

Specific files:
- `observability/otel.ts` line ~109: `Effect.runPromise(program.pipe(` -- rewrite to show `program.pipe(Effect.provide(...))` as a composable unit
- `observability/context.ts` line ~113: `await Effect.runPromise(program)` -- rewrite to show Effect composition
- `effect/index.ts` line ~18: `const result = await Effect.runPromise(` -- rewrite to show the helper being used within Effect.gen

These are documentation-only changes. No runtime behavior changes.
  </action>
  <verify>
    <automated>bun test && bun run build</automated>
  </verify>
  <done>All three files have JSDoc examples showing idiomatic Effect composition. No runPromise appears in JSDoc examples. Build and full test suite pass.</done>
</task>

</tasks>

<verification>
1. `bun test` -- full test suite passes
2. `bun run build` -- build succeeds
3. `grep -rn "runPromise" packages/core/src/observability/otel.ts packages/core/src/observability/context.ts packages/core/src/effect/index.ts` -- no matches (all were in JSDoc, now rewritten)
4. `bun test tests/unit/core/migration/boundary-guard.test.ts` -- still green (confirms guard handles these files correctly)
</verification>

<success_criteria>
- JSDoc examples in all 3 files use idiomatic Effect patterns
- No runPromise references remain in documentation examples
- Full test suite and build pass
- Phase 49 CONS-04 requirement fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/49-peripheral-boundary-migration/49-02-SUMMARY.md`
</output>
