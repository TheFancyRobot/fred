---
phase: 46-showcase-examples
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/examples/examples-guard.test.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Examples guard fails when any example TypeScript source no longer compiles"
    - "Examples guard still enforces required example structure for all expected examples"
    - "Examples guard still enforces package-import policy (no relative internal imports)"
  artifacts:
    - path: "tests/unit/examples/examples-guard.test.ts"
      provides: "Guard test with per-example compile assertions plus existing structure/import checks"
      contains: "bunx tsc --noEmit"
  key_links:
    - from: "tests/unit/examples/examples-guard.test.ts"
      to: "examples/*/tsconfig.json"
      via: "per-example tsc invocation"
      pattern: "--noEmit -p"
    - from: "tests/unit/examples/examples-guard.test.ts"
      to: "examples/*/src/**/*.ts"
      via: "existing import-policy assertions retained"
      pattern: "@fancyrobot/fred"
---

<objective>
Close the Phase 46 verification gap by extending the examples guard to enforce compile safety per example while preserving current structural and import-policy assertions.

Purpose: The current guard only checks folder shape and import patterns, so TypeScript regressions in examples can pass unnoticed. This plan wires compile validation directly into the guardrail so examples stay green over time.

Output: Updated `examples-guard.test.ts` with per-example typecheck coverage and passing verification commands.
</objective>

<execution_context>
@/home/gimbo/.claude/get-shit-done/workflows/execute-plan.md
@/home/gimbo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-showcase-examples/46-VERIFICATION.md
@.planning/phases/46-showcase-examples/46-08-SUMMARY.md
@tests/unit/examples/examples-guard.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add per-example TypeScript compile checks to examples guard</name>
  <files>tests/unit/examples/examples-guard.test.ts</files>
  <action>
    Extend the guard test to typecheck each expected example package individually using its own `tsconfig.json`.

    Implementation requirements:
    1. Keep the current structure assertions (required files/directories) intact.
    2. Keep the current import-policy assertions intact (`@fancyrobot/fred` package imports, no forbidden relative paths).
    3. Add a dedicated compile-safety assertion that loops `EXPECTED_EXAMPLES` and runs TypeScript compile per example (for example: `bunx tsc --noEmit -p examples/<name>/tsconfig.json`, or equivalent programmatic compile call).
    4. Make the test fail with actionable output naming the specific example(s) that failed compilation.
    5. Do not replace or weaken existing assertions; compile safety is additive.
  </action>
  <verify>
    <automated>bun test tests/unit/examples/examples-guard.test.ts</automated>
  </verify>
  <done>`tests/unit/examples/examples-guard.test.ts` contains a per-example compile check and still contains structural/import assertions from the existing guard.</done>
</task>

<task type="auto">
  <name>Task 2: Verify guardrail behavior remains complete</name>
  <files>tests/unit/examples/examples-guard.test.ts</files>
  <action>
    Run verification after the guard update and fix any assertion regressions in the guard implementation.

    Required checks:
    1. `bun test tests/unit/examples/examples-guard.test.ts` passes.
    2. `bun test` passes so the stricter guard integrates cleanly with the suite.

    If failures occur, adjust only the guard logic to restore all three protections: structure, import policy, and per-example compile safety.
  </action>
  <verify>
    <automated>bun test tests/unit/examples/examples-guard.test.ts && bun test</automated>
  </verify>
  <done>Guard test passes in isolation and in full-suite execution, with compile safety enforced in addition to existing structure/import checks.</done>
</task>

</tasks>

<verification>
- `bun test tests/unit/examples/examples-guard.test.ts` passes
- `bun test` passes
- `tests/unit/examples/examples-guard.test.ts` still checks structure/import policy and now checks per-example compile safety
</verification>

<success_criteria>
1. Any TypeScript error in an example `src/` file causes `examples-guard.test.ts` to fail.
2. Existing structure and import-policy guard assertions remain active and passing.
3. The phase truth "Guardrails keep examples green over time, including compile safety" is now satisfiable by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/46-showcase-examples/46-09-SUMMARY.md`
</output>
