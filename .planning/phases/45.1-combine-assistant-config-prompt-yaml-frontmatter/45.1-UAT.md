---
status: testing
phase: 45.1-combine-assistant-config-prompt-yaml-frontmatter
source: 45.1-01-SUMMARY.md, 45.1-02-SUMMARY.md, 45.1-03-SUMMARY.md
started: 2026-03-01T23:10:00Z
updated: 2026-03-01T23:50:00Z
---

## Current Test

number: 6
name: Markdown agent file parsed correctly
expected: |
  TUI loads with test-agent showing correct model from agents/test-agent.md.
  Sending a message gets a response from the markdown-defined agent.
awaiting: user response (needs valid API key for chosen platform)

## Tests

### 1. Unit tests pass for agent file loader
expected: Running `bun test tests/unit/core/agent/file-loader.test.ts` passes all 18 tests.
result: pass

### 2. Unit tests pass for config integration
expected: Running `bun test tests/unit/core/config/agent-file-integration.test.ts` passes all integration tests.
result: pass

### 3. Unit tests pass for file watcher
expected: Running `bun test tests/unit/core/agent/file-watcher.test.ts` passes all tests.
result: pass

### 4. Full test suite passes
expected: Running `bun test` completes with no failures (1669/1669).
result: pass

### 5. Build succeeds
expected: Running `bun run build` completes with no errors.
result: pass

### 6. Markdown agent file parsed correctly
expected: TUI loads with test-agent showing correct model. Sending a message gets a response.
result: [pending]
notes: TUI now shows correct model (gpt4o). Blocked on OPENAI_API_KEY not set — user needs to set API key or switch agent platform.

### 7. Plain markdown files skipped
expected: A `.md` file without opening `---` frontmatter delimiters is silently skipped.
result: [pending]

### 8. Duplicate ID detection at startup
expected: If a markdown-defined agent and a config-defined agent share the same `id`, startup fails with clear error.
result: [pending]

### 9. Hot reload updates agent on file change
expected: Editing a markdown agent file hot-reloads the agent without restart.
result: [pending]

### 10. Invalid reload is non-fatal
expected: Saving invalid markdown agent file logs warning, doesn't crash.
result: [pending]

## Summary

total: 10
passed: 5
issues: 0
pending: 5
skipped: 0

## Bugs Found During UAT

These bugs were discovered and fixed during testing (not in phase 45.1 code, but exposed by it):

### Bug 1: TUI didn't detect YAML config files
- Files: packages/cli/src/project/detect.ts, types.ts, resolve-config.ts
- Fix: Added fred.config.yaml and fred.config.yml to CONFIG_FILENAMES and ConfigFormat type
- Tests added: 3 new tests in project-detection.test.ts

### Bug 2: Agents lost after routing config (runtime invalidation ordering)
- File: packages/core/src/config/initializer.ts
- Cause: configureRouting() calls invalidateRuntime() which rebuilds the Effect runtime, destroying agents already created
- Fix: Moved configureRouting and configureWorkflows to run BEFORE agent creation

## Gaps

[none yet]
