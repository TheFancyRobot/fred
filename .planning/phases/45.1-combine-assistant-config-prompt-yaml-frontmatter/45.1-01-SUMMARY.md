---
phase: 45.1-combine-assistant-config-prompt-yaml-frontmatter
plan: 01
subsystem: api
tags: [typescript, agent-config, yaml-frontmatter, js-yaml, file-discovery]

# Dependency graph
requires:
  - phase: 45
    provides: Effect-only public API surface and stabilized agent/service contracts
provides:
  - Core markdown agent loader (`parseAgentFile`, `validateAgentFrontmatter`, `toAgentConfig`, `discoverAgentFiles`, `loadAgentFiles`)
  - Tagged parse/validation error (`AgentFileParseError`) in shared agent error union
  - Unit test coverage for parsing, validation, conversion, recursive discovery, and load pipeline orchestration
affects: [45.1-02-config-integration, 45.1-03-hot-reload]

# Tech tracking
tech-stack:
  added: []
  patterns: [yaml-frontmatter-parsing, plain-prompt-md-skip, recursive-agent-dir-discovery]

key-files:
  created:
    - packages/core/src/agent/file-loader.ts
    - tests/unit/core/agent/file-loader.test.ts
  modified:
    - packages/core/src/agent/errors.ts

key-decisions:
  - "Markdown files without opening frontmatter delimiters are treated as plain prompt files and skipped (return null)"
  - "Unknown frontmatter keys are ignored while known AgentConfig fields are strictly validated"

patterns-established:
  - "Agent .md parsing enforces explicit required fields (id/platform/model) plus non-empty body prompt"
  - "File loading pipeline is discover -> parse -> validate -> convert with AgentFileParseError for invalid definitions"

requirements-completed: [MDAGENT-01, MDAGENT-02]

# Metrics
duration: 2min
completed: 2026-03-01
---

# Phase 45.1 Plan 01: Core Agent File Loader Summary

**YAML-frontmatter markdown agent parsing with strict validation, plain-prompt fallback behavior, and recursive .md discovery for startup loading**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-01T22:38:41Z
- **Completed:** 2026-03-01T22:40:29Z
- **Tasks:** 1 (TDD: RED + GREEN)
- **Files modified:** 3

## Accomplishments
- Added `packages/core/src/agent/file-loader.ts` with `parseAgentFile`, `validateAgentFrontmatter`, `toAgentConfig`, `discoverAgentFiles`, and `loadAgentFiles`
- Added `AgentFileParseError` in `packages/core/src/agent/errors.ts` and included it in `AgentError`
- Implemented comprehensive unit tests in `tests/unit/core/agent/file-loader.test.ts` covering parser, validation, conversion, recursive discovery, and full load pipeline
- Verified targeted tests and full workspace build both pass

## Task Commits

Each task was committed atomically:

1. **Task 1 (RED): Add failing tests for agent markdown file loader** - `aa3abde` (test)
2. **Task 1 (GREEN): Implement parsing/validation/discovery/load pipeline** - `6043652` (feat)

## Files Created/Modified
- `packages/core/src/agent/file-loader.ts` - Core parser/validator/converter/discovery loader for markdown agent definitions
- `packages/core/src/agent/errors.ts` - Added tagged `AgentFileParseError` and included in `AgentError` union
- `tests/unit/core/agent/file-loader.test.ts` - TDD coverage for valid/invalid frontmatter, required fields, type validation, conversion, and recursive scanning

## Decisions Made
- Used `js-yaml` `yaml.load` for frontmatter parsing and enforced object-only frontmatter payloads
- Frontmatter `systemMessage` key is rejected with a clear error because markdown body is the sole prompt source
- Discovery includes only `.md` files, recursively, and silently skips missing directories

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Ready for `45.1-02-PLAN.md` (Config integration) with core file loading primitives available
- Parsing behavior and error surface are now stable for initializer integration and coexistence validation

## Self-Check: PASSED

- FOUND: `packages/core/src/agent/file-loader.ts`
- FOUND: `packages/core/src/agent/errors.ts` includes `AgentFileParseError`
- FOUND: `tests/unit/core/agent/file-loader.test.ts`
- FOUND: commit `aa3abde` (RED)
- FOUND: commit `6043652` (GREEN)
- `bun test tests/unit/core/agent/file-loader.test.ts`: PASS (18/18)
- `bun run build`: PASS

---
*Phase: 45.1-combine-assistant-config-prompt-yaml-frontmatter*
*Completed: 2026-03-01*
