---
phase: 45-public-api-surface-verification
plan: 03
type: execute
wave: 3
depends_on:
  - 45-02
files_modified:
  - .changeset/v0.3.0-effect-migration.md
  - packages/core/src/index.ts
autonomous: true
requirements:
  - API-04
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04

must_haves:
  truths:
    - "v0.3.0 changeset exists documenting all breaking changes with before/after code examples"
    - "bun run build succeeds with zero errors across all packages"
    - "Build smoke test confirms sub-path imports (@fancyrobot/fred, @fancyrobot/fred/eval, @fancyrobot/fred/effect, @fancyrobot/fred/tools) resolve correctly"
    - "bun test passes with zero failures"
    - "No @ts-ignore or @ts-expect-error workaround annotations exist in packages/core/src/"
    - "Boundary guard test passes confirming no new Effect.runPromise violations"
    - "All pre-existing LSP errors are fixed or explicitly documented"
  artifacts:
    - path: ".changeset/v0.3.0-effect-migration.md"
      provides: "v0.3.0 breaking change changeset with migration guidance"
      contains: "\"@fancyrobot/fred\": major"
  key_links:
    - from: ".changeset/v0.3.0-effect-migration.md"
      to: ".changeset/config.json"
      via: "Changesets configuration"
      pattern: "baseBranch.*main"
---

<objective>
Create the v0.3.0 changeset documenting breaking changes, fix any pre-existing type errors, and run the full verification sweep (build, test, typecheck, boundary guard).

Purpose: This is the verification gate for the v0.3.0 milestone. Every requirement from the milestone must be satisfied. The changeset provides migration guidance for external consumers.

Output: v0.3.0 changeset file, verified clean build and test suite, documented resolution of all pre-existing issues.
</objective>

<execution_context>
@/home/gimbo/.claude/get-shit-done/workflows/execute-plan.md
@/home/gimbo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/45-public-api-surface-verification/45-01-SUMMARY.md
@.planning/phases/45-public-api-surface-verification/45-02-SUMMARY.md
@.changeset/config.json
@packages/core/src/index.ts
@tests/unit/core/migration/phase-44-boundary-guard.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix pre-existing type errors and remove workaround annotations</name>
  <files>packages/core/src/index.ts</files>
  <action>
    1. Run `bunx tsc --noEmit --project packages/core/tsconfig.json 2>&1` (or equivalent -- check if tsconfig exists, otherwise run from project root). Capture all type errors.

    2. If tsconfig.json does not exist for the core package, run `bunx tsc --noEmit` from the project root, filtering for packages/core errors.

    3. For each type error found:
       - If it is an Effect yield error in index.ts (known pre-existing): fix the type by adding explicit type annotations or adjusting Effect.gen usage
       - If it is a tracing import error: fix the import path or type reference
       - If it is a config/initializer reference error: fix the reference
       - Document each fix

    4. Search for `@ts-ignore` and `@ts-expect-error` annotations in `packages/core/src/`:
       ```
       grep -rn "@ts-ignore\|@ts-expect-error" packages/core/src/
       ```
       If any are found, remove them and fix the underlying type issue.

    5. Also check `packages/cli/src/` and `packages/dev/src/` for migration-related `@ts-ignore`/`@ts-expect-error`:
       ```
       grep -rn "@ts-ignore\|@ts-expect-error" packages/cli/src/ packages/dev/src/
       ```
       Fix any that were added during the v0.3.0 migration phases.

    6. Run `bun run build` to verify build still succeeds after fixes.

    7. Run build smoke test to verify dist output has correct exports:
       After `bun run build` succeeds, verify that the package.json `exports` field entries resolve correctly by importing key sub-paths from the built package. Run each of the following import checks (adjust sub-path list to match the exports field entries added in Plan 02):
       ```bash
       # Verify main entrypoint resolves
       bun -e "import { Fred } from '@fancyrobot/fred'; console.log('main: OK')"
       # Verify eval sub-path resolves
       bun -e "import { EvaluationService } from '@fancyrobot/fred/eval'; console.log('eval: OK')"
       # Verify effect sub-path resolves
       bun -e "import { FredLayers } from '@fancyrobot/fred/effect'; console.log('effect: OK')"
       # Verify tools sub-path resolves
       bun -e "import { createCalculatorTool } from '@fancyrobot/fred/tools'; console.log('tools: OK')"
       ```
       If any import fails with a module-not-found error, fix the corresponding entry in `packages/core/package.json` `"exports"` field or the barrel file it points to. Note: Bun workspace resolution may resolve these even without the exports field -- if all pass, also verify by inspecting the `dist/` output directly to confirm the expected entry files exist.

    8. Run `bun test` to verify no regressions.
  </action>
  <verify>
    <automated>bun run build && bun -e "import { Fred } from '@fancyrobot/fred'; import { EvaluationService } from '@fancyrobot/fred/eval'; console.log('smoke: OK')" && bun test</automated>
  </verify>
  <done>Zero @ts-ignore/@ts-expect-error annotations in core/src, build succeeds, build smoke test confirms sub-path exports resolve correctly, tests pass, pre-existing type errors fixed or documented with specific issue references</done>
</task>

<task type="auto">
  <name>Task 2: Create v0.3.0 changeset and run full verification sweep</name>
  <files>.changeset/v0.3.0-effect-migration.md</files>
  <action>
    1. Create `.changeset/v0.3.0-effect-migration.md` with the following content. This is a changesets-format file with YAML frontmatter specifying package bump levels, followed by markdown changelog body.

       The changeset must:
       - Mark `@fancyrobot/fred` as `major` (bumps 0.2.x to 0.3.0)
       - Mark `@fancyrobot/fred-cli` as `minor` (import path updates)
       - Mark `@fancyrobot/fred-dev` as `patch` (import path updates)
       - Document ALL breaking changes:
         a. Removed imperative manager classes (ToolRegistry, AgentManager, PipelineManager, ContextManager, HookManager, ProviderRegistry, MessageRouter, MessageProcessor Promise wrappers)
         b. Removed imperative class exports (IntentMatcher class, IntentRouter class, WorkflowManager class)
         c. New Effect service tags as replacements
         d. New sub-path imports (@fancyrobot/fred/eval, /context/sqlite, /context/postgres, /tools)
         e. package.json exports field restricting deep imports
       - Include before/after code examples for:
         a. Using Effect services instead of imperative classes
         b. Using Fred class (still works, now delegates to Effect internally)
         c. Importing eval types from sub-path
         d. Importing storage from sub-path
       - Audience: external consumers unfamiliar with migration context
       - No "why this change" motivation section -- just facts

    2. Verify the changeset file format is valid:
       - YAML frontmatter between `---` markers
       - Package names match exactly what is in each package.json `"name"` field
       - Check package names: `grep '"name"' packages/*/package.json`

    3. Run the full verification sweep:
       a. `bun run build` -- all packages build with zero errors
       b. `bun test` -- all tests pass with zero failures
       c. `bun test tests/unit/core/migration/phase-44-boundary-guard.test.ts` -- boundary guard passes
       d. `grep -rn "@ts-ignore\|@ts-expect-error" packages/core/src/` -- zero matches
       e. Verify exports surface:
          - `grep -n "IntentMatcher\|IntentRouter\|WorkflowManager" packages/core/src/exports.ts` -- zero class export lines
          - `grep -n "WorkflowService" packages/core/src/services.ts` -- present in layer composition
       f. Verify changeset exists and is well-formed:
          - `cat .changeset/v0.3.0-effect-migration.md | head -5` shows YAML frontmatter

    4. If any verification step fails, fix the issue and re-run. Document any issues found and their resolutions.
  </action>
  <verify>
    <automated>bun run build && bun test && bun test tests/unit/core/migration/phase-44-boundary-guard.test.ts</automated>
  </verify>
  <done>v0.3.0 changeset exists with complete migration documentation, build passes, all tests pass, boundary guard passes, zero workaround annotations remain</done>
</task>

</tasks>

<verification>
Full Phase 45 verification checklist:

**API Surface (API-01, API-02, API-03):**
- [ ] `grep "export.*class.*IntentMatcher\|export.*class.*IntentRouter\|export.*class.*WorkflowManager" packages/core/src/exports.ts` returns nothing
- [ ] `grep "WorkflowService\|IntentMatcherService\|MessageRouterService\|ToolGateService\|CheckpointService\|PauseService" packages/core/src/index.ts` shows re-exports
- [ ] `grep "WorkflowService" packages/core/src/services.ts` shows layer wiring

**Breaking Change Documentation (API-04):**
- [ ] `.changeset/v0.3.0-effect-migration.md` exists
- [ ] Contains `"@fancyrobot/fred": major`
- [ ] Contains before/after code examples

**Build & Test (TEST-01, TEST-02, TEST-03):**
- [ ] `bun run build` exits 0
- [ ] Build smoke test: `bun -e "import { Fred } from '@fancyrobot/fred'; import { EvaluationService } from '@fancyrobot/fred/eval'; console.log('OK')"` exits 0
- [ ] `bun test` exits 0 with zero failures

**LSP/Type Errors (TEST-04):**
- [ ] `grep -rn "@ts-ignore\|@ts-expect-error" packages/core/src/` returns nothing
- [ ] Pre-existing LSP errors documented or fixed

**Boundary Guard:**
- [ ] `bun test tests/unit/core/migration/phase-44-boundary-guard.test.ts` passes
</verification>

<success_criteria>
1. v0.3.0 changeset file exists with correct frontmatter and complete migration documentation
2. `bun run build` succeeds with zero errors and build smoke test confirms sub-path exports resolve
3. `bun test` passes with zero failures
4. Boundary guard test passes
5. Zero @ts-ignore/@ts-expect-error annotations in packages/core/src/
6. All 8 Phase 45 requirements verified: API-01, API-02, API-03, API-04, TEST-01, TEST-02, TEST-03, TEST-04
</success_criteria>

<output>
After completion, create `.planning/phases/45-public-api-surface-verification/45-03-SUMMARY.md`
</output>
