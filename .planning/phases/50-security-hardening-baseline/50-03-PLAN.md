---
phase: 50-security-hardening-baseline
plan: 03
type: execute
wave: 2
depends_on: ["50-01", "50-02"]
files_modified:
  - .github/workflows/security.yml
  - .planning/phases/50-security-hardening-baseline/50-VALIDATION.md
autonomous: true
requirements: [SEC-04, SEC-05]

must_haves:
  truths:
    - "CI runs gitleaks secret scan on pull requests to main"
    - "CI runs semgrep SAST scan on pull requests to main"
    - "CI runs dependency audit on pull requests to main"
    - "Validation checklist documents local commands matching CI behavior"
  artifacts:
    - path: ".github/workflows/security.yml"
      provides: "GitHub Actions workflow with gitleaks, semgrep, and dependency audit jobs"
      contains: "gitleaks"
    - path: ".planning/phases/50-security-hardening-baseline/50-VALIDATION.md"
      provides: "Security validation runbook with local + CI commands"
  key_links:
    - from: ".github/workflows/security.yml"
      to: "GitHub Actions CI"
      via: "on.pull_request trigger"
      pattern: "pull_request"
---

<objective>
Add CI security scanning workflow (gitleaks, semgrep, dependency audit) and finalize the security validation runbook with concrete local and CI commands.

Purpose: The CI pipeline has no secret scanning, SAST, or dependency auditing. The validation checklist needs concrete commands. This closes SEC-04 and SEC-05.
Output: Security CI workflow file and finalized validation documentation.
</objective>

<execution_context>
@/home/gimbo/.claude/get-shit-done/workflows/execute-plan.md
@/home/gimbo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-security-hardening-baseline/50-CONTEXT.md
@.planning/phases/50-security-hardening-baseline/50-RESEARCH.md
@.planning/phases/50-security-hardening-baseline/50-01-SUMMARY.md
@.planning/phases/50-security-hardening-baseline/50-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CI security scanning workflow</name>
  <files>.github/workflows/security.yml</files>
  <action>
    Create `.github/workflows/security.yml` with three jobs triggered on pull_request to main:

    **Job 1: gitleaks (Secret Scan)**
    - uses: actions/checkout@v4 with fetch-depth: 0 (full history for diff scanning)
    - uses: gitleaks/gitleaks-action@v2
    - env: GITHUB_TOKEN from secrets.GITHUB_TOKEN
    - Note: Add comment that org repos may need GITLEAKS_LICENSE secret

    **Job 2: semgrep (SAST Scan)**
    - runs-on: ubuntu-latest
    - container: image: semgrep/semgrep
    - uses: actions/checkout@v4
    - run: `semgrep scan --config auto --severity ERROR --error --json --output semgrep-results.json .`
    - Upload semgrep-results.json as artifact (if: always())
    - Use `--severity ERROR` to filter to high-confidence findings only (per research pitfall #5)

    **Job 3: dependency-audit (Dependency Audit)**
    - uses: actions/checkout@v4
    - uses: oven-sh/setup-bun@v2 with bun-version: latest
    - run: bun install --frozen-lockfile
    - run: bun audit --audit-level=high
    - Note: bun audit exits non-zero on high/critical vulnerabilities

    Add a workflow-level `name: Security` and ensure all three jobs run independently (not dependent on each other).
  </action>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/security.yml'))" && echo "Valid YAML"</automated>
  </verify>
  <done>Security CI workflow exists with gitleaks, semgrep, and dependency audit jobs. YAML is valid. All three jobs trigger on PR to main.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize validation runbook and run full verification</name>
  <files>.planning/phases/50-security-hardening-baseline/50-VALIDATION.md</files>
  <action>
    Update `50-VALIDATION.md` to be a complete validation runbook:

    1. Update frontmatter: set `nyquist_compliant: true`, `wave_0_complete: true`.

    2. Add a "Local Validation Commands" section with exact commands a developer can run:
       - Auth/CORS: `bun test tests/unit/dev/security.test.ts`
       - Rate limiting: `bun test tests/unit/dev/rate-limiter.test.ts`
       - MCP trust boundaries: `bun test tests/unit/mcp/security.test.ts`
       - Full security test suite: `bun test tests/unit/dev tests/unit/mcp`
       - Build verification: `bun run build`
       - Full test suite: `bun test`
       - CORS wildcard check: `grep -r "Access-Control-Allow-Origin.*\*" packages/dev/` (should return nothing)
       - MCP env spread check: `grep -n "\.\.\.process\.env" packages/core/src/mcp/stdio-transport.ts` (should return nothing)

    3. Add a "CI Validation" section explaining the security.yml workflow and what each job checks.

    4. Update the per-task verification map with final status for all tasks.

    5. Mark validation sign-off checklist items as complete.

    6. Run full verification: `bun test && bun run build` to confirm everything is green.
  </action>
  <verify>
    <automated>bun test && bun run build</automated>
  </verify>
  <done>Validation runbook has concrete local and CI commands. Full test suite passes. Build succeeds. All SEC requirements are covered.</done>
</task>

</tasks>

<verification>
- `.github/workflows/security.yml` is valid YAML with 3 jobs
- `50-VALIDATION.md` has local commands matching CI behavior
- `bun test` full suite passes
- `bun run build` succeeds
</verification>

<success_criteria>
- CI workflow triggers on PRs to main with gitleaks, semgrep, and dependency audit
- Validation runbook documents exact local and CI commands for all hardening controls
- Full test suite and build remain green
</success_criteria>

<output>
After completion, create `.planning/phases/50-security-hardening-baseline/50-03-SUMMARY.md`
</output>
