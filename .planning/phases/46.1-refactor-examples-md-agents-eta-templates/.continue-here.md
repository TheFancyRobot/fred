---
phase: 46.1-refactor-examples-md-agents-eta-templates
task: discuss-phase
total_tasks: 1
status: in_progress
last_updated: 2026-03-03T06:02:11.020Z
---

<current_state>
Running `/gsd:discuss-phase 46.1` to update the existing CONTEXT.md. User selected all 4 gray areas to discuss: Agent loading approach, ETA feature mapping, Example 09 treatment, Programmatic remnants. Partway through discussion — 2 of 4 areas complete, 2 remaining.
</current_state>

<completed_work>

- CONTEXT.md already exists from a prior session (gathered 2026-03-03)
- User chose to "Update it" — revisiting decisions
- Prior context loaded (PROJECT.md, REQUIREMENTS.md, STATE.md, prior CONTEXT.md files from 45.1, 45.2, 46)
- Codebase scouted: all 12 examples surveyed, agent creation patterns mapped, config.yaml files reviewed, file-loader.ts and initializer.ts investigated

### Area 1: Agent loading approach — RESOLVED (technical answer, not user preference)
Key finding: `Fred.create()` does NOT auto-scan for agent files. Auto-discovery only happens via `initializeFromConfig()` which checks for `./agents/` relative to config file (initializer.ts:222-225).
- **Decision:** Every example needs a minimal config.yaml + `initializeFromConfig()` call
- If `./agents/` exists next to config file, it's scanned automatically — no `agentDirs` needed in config
- Examples 10 and 12 already have config.yaml — remove `agents[]` sections, agents/ auto-discovered
- Examples 01-09, 11 need a minimal config.yaml (at minimum: provider declaration)
- Example 10's `programmatic-equivalent.ts` keeps `createAgent()` calls as the Rosetta Stone reference

### Area 2: ETA feature mapping — RESOLVED
- **ETA density:** Natural fit only — plain markdown fine for simple agents, no forced templating
- **Feature placement:** Claude distributes for best natural coverage (user gave full discretion)
- **Partials:** Per-example `partials/` directories (self-contained, preserves copy-and-run packaging)
- **Per-message variables:** Show BOTH patterns across examples — frontmatter `vars:` in one, `fred.setGlobalVariable()` in another
</completed_work>

<remaining_work>

### Area 3: Example 09 treatment — PARTIALLY DISCUSSED
- Example 09 (eval harness) has NO createAgent() calls — it's pure eval API
- User wanted to understand what Example 09 is before deciding. Shown the README.
- User said "You decide" on whether to add agent .md files as reference documentation
- Decision not yet recorded: Claude should determine whether adding agent .md adds clarity or confusion
- User interrupted before this was finalized — need to record Claude's decision

### Area 4: Programmatic remnants — NOT YET DISCUSSED
- What programmatic code stays in .ts files after migration?
- Tool definitions (Effect Schema, handler functions) stay in code
- Intents, hooks, pipeline config, routing — all remain programmatic
- Only agent definitions (systemMessage + config) move to .md
- May not need much discussion — largely already covered in existing CONTEXT.md decisions

### After discussion completes:
- Update CONTEXT.md with refined decisions from this session
- Commit updated CONTEXT.md
- Update STATE.md
</remaining_work>

<decisions_made>

- Agent file discovery requires `initializeFromConfig()` — not automatic from `Fred.create()`
- Every example gets a minimal config.yaml to enable agent auto-discovery
- No `agentDirs` needed in config if `./agents/` convention followed
- ETA features distributed by Claude for natural coverage, not forced per-file
- Per-example partials/ directories (self-contained packaging)
- Show both variable patterns: frontmatter vars: AND globalVariables across different examples
- Example 10 programmatic-equivalent.ts keeps createAgent() as reference
</decisions_made>

<blockers>
- None — discussion was progressing normally
</blockers>

<context>
User's philosophy: technical questions about how Fred works should be answered by reading the implementation, not asked as preference questions. Only genuine user-preference gray areas should be presented for discussion. The agent loading approach was resolved by reading ConfigInitializer source code rather than asking the user to choose.

Existing CONTEXT.md at 46.1-CONTEXT.md is largely correct but needs updates to reflect:
1. The agent loading mechanism (initializeFromConfig + config.yaml requirement)
2. ETA variable patterns decision (both frontmatter vars and globalVariables)
3. Example 09 final treatment
4. Any refinements from remaining discussion areas
</context>

<next_action>
Resume with `/gsd:discuss-phase 46.1` and select "Update it" when prompted.
- Finalize Example 09 decision (Claude decides: likely add .md as reference documentation for completeness)
- Quickly cover Area 4 (programmatic remnants) — may just confirm existing CONTEXT.md decisions
- Update 46.1-CONTEXT.md with all refined decisions
- Commit and update STATE.md
</next_action>
